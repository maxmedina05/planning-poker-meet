# --- Local-only files — generated by deploy.sh or filled in from examples ---
config.js
deployment.json
privacy.html

# --- Secrets — never commit these ---

# Service account / backend credentials (future phases)
service-account.json
*-credentials.json
*.pem
*.key

# Environment files
.env
.env.*
!.env.example

# Firebase
.firebaserc
.firebase/
firebase-debug.log
firestore-debug.log

# --- Editor / OS noise ---
.DS_Store
Thumbs.db
.vscode/settings.json
*.swp
